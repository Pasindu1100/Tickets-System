# --- Dependencies ---
node_modules/
package-lock.json # Optional: Keep this if you want to lock versions, ignore if you use yarn exclusively
yarn.lock # Optional: Keep if using Yarn, ignore if using npm exclusively

# --- Environment Variables (CRITICAL) ---
# Contains DB connection strings and API keys
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# --- Build Outputs ---
# React build output
build/
dist/

# --- Logs ---
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- Runtime Data ---
# If you run MongoDB locally and store data in the project (rare but possible)
data/db
*.pid
*.seed
*.pid.lock

# --- Testing & Coverage ---
coverage/
.nyc_output/

# --- IDE & System Files ---
.DS_Store
.vscode/
.idea/
*.swp
*.swo

# --- Optional: Miscellaneous ---
.eslintcache